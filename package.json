{
  "name": "tapkit-mcp",
  "version": "1.3.0",
  "description": "MCP server for securely viewing and controlling iOS devices with AI clients",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "node --import tsx --test tests/auth.test.ts"
  },
  "keywords": ["mcp", "tapkit", "ios", "openai", "codex", "claude", "automation"],
  "author": "TapKit",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
